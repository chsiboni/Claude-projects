# מדריך הקמה — "עברית ישרה"

כל מה שצריך כדי לפתוח את הפרויקט הזה בתיקייה חדשה במחשב שלך, להריץ אותו, ובמידת הצורך להעלות אותו לאינטרנט.

הפרויקט הוא כלי קטן שמציג טקסט עברי מ‑Claude בצורה **ישרה ונכונה** (מימין לשמאל), בעיצוב שנראה כמו שיחה רגילה ב‑Claude, עם תמיכה בכותרות, הדגשות, רשימות וקוד.

---

## מה צריך להתקין מראש

| כלי | בשביל מה | קישור |
|-----|----------|-------|
| **דפדפן** (Chrome / Edge / Safari) | להריץ ולראות את הכלי | כבר מותקן אצלך |
| **עורך קוד** — מומלץ VS Code | לערוך את הקבצים | https://code.visualstudio.com |
| **Node.js** (גרסה 18 ומעלה) | *רק אם רוצים שרת מקומי או Railway* | https://nodejs.org |
| **Git** | *רק אם רוצים לשכפל מ‑GitHub* | https://git-scm.com |

> שים לב: אם רק רוצים לפתוח את הכלי בדפדפן — **לא צריך שום התקנה**. Node ו‑Git נחוצים רק למסלול השרת/Railway.

---

## מבנה התיקייה

```
hebrew-yeshara/
├── index.html      ← האפליקציה עצמה (כל העיצוב והקוד בפנים)
├── server.js       ← שרת Node קטן (רק ל‑Railway / הרצה כשרת)
├── package.json    ← הגדרות הפרויקט ופקודת ההרצה
├── railway.json    ← הגדרות פריסה ל‑Railway
├── .gitignore      ← קבצים שלא נשמרים ב‑Git
└── README.md       ← תיאור קצר
```

הקובץ היחיד שחייבים הוא `index.html`. כל השאר נחוץ רק אם רוצים להריץ כשרת או להעלות ל‑Railway.

---

## מסלול א' — שכפול מ‑GitHub (הכי מהיר)

הקוד כבר נמצא ב‑repo שלך. פותחים טרמינל (או Terminal ב‑VS Code) ומריצים:

```bash
git clone https://github.com/chsiboni/Claude-projects.git hebrew-yeshara
cd hebrew-yeshara
git checkout claude/hebrew-text-converter-f01ql0
```

זהו — כל הקבצים אצלך בתיקייה `hebrew-yeshara`.

---

## מסלול ב' — בנייה מאפס

אם אתה מעדיף לבנות בעצמך תיקייה חדשה:

1. צור תיקייה חדשה בשם `hebrew-yeshara` ופתח אותה ב‑VS Code.
2. את קובץ ה‑**index.html** קח מהקובץ ששלחתי לך (`hebrew-yeshara.html`) — פשוט העתק אותו לתיקייה ושנה את שמו ל‑`index.html`.
   *(או שכפל מ‑GitHub לפי מסלול א' והעתק משם רק את `index.html`.)*
3. צור את שאר הקבצים הקטנים לפי התוכן המלא שמופיע בהמשך המסמך.

### `server.js`

```js
// Minimal zero-dependency static server for Railway.
// Serves index.html for the app; binds to the port Railway provides.
const http = require("http");
const fs = require("fs");
const path = require("path");

const PORT = process.env.PORT || 3000;
const ROOT = __dirname;

const TYPES = {
  ".html": "text/html; charset=utf-8",
  ".css": "text/css; charset=utf-8",
  ".js": "text/javascript; charset=utf-8",
  ".svg": "image/svg+xml",
  ".ico": "image/x-icon",
  ".png": "image/png",
  ".json": "application/json; charset=utf-8",
};

const server = http.createServer((req, res) => {
  let urlPath = decodeURIComponent((req.url || "/").split("?")[0]);
  if (urlPath === "/") urlPath = "/index.html";

  const filePath = path.join(ROOT, path.normalize(urlPath));
  if (!filePath.startsWith(ROOT)) {
    res.writeHead(403);
    res.end("Forbidden");
    return;
  }

  fs.readFile(filePath, (err, data) => {
    if (err) {
      fs.readFile(path.join(ROOT, "index.html"), (e2, home) => {
        if (e2) {
          res.writeHead(404);
          res.end("Not found");
          return;
        }
        res.writeHead(200, { "Content-Type": TYPES[".html"] });
        res.end(home);
      });
      return;
    }
    const type = TYPES[path.extname(filePath)] || "application/octet-stream";
    res.writeHead(200, { "Content-Type": type });
    res.end(data);
  });
});

server.listen(PORT, () => {
  console.log(`עברית ישרה running on port ${PORT}`);
});
```

### `package.json`

```json
{
  "name": "hebrew-yeshara",
  "version": "1.0.0",
  "description": "עברית ישרה — a Claude-style RTL viewer for Hebrew text",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
```

### `railway.json`

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
```

### `.gitignore`

```
node_modules/
npm-debug.log
.DS_Store
.env
```

---

## איך מריצים

### הדרך הכי פשוטה — בלי שרת
לחץ פעמיים על `index.html`. הוא ייפתח בדפדפן ויעבוד לגמרי, גם בלי אינטרנט.

> אם כפתור "הדבקה" האוטומטי לא עובד בפתיחה מקומית (הדפדפן חוסם קריאה מהלוח) — פשוט הדבק בתיבה עם **Ctrl+V** ולחץ **Enter**. זה תמיד עובד.

### כשרת מקומי (דורש Node)
בטרמינל, בתוך התיקייה:

```bash
npm start
```

ואז פתח בדפדפן: http://localhost:3000

---

## העלאה ל‑Railway (אתר חי באינטרנט)

1. העלה את התיקייה ל‑GitHub (אם עוד לא שם).
2. היכנס ל‑https://railway.app והתחבר עם GitHub.
3. **New Project → Deploy from GitHub repo** ובחר את הריפו.
4. בחר את הענף הרצוי.
5. Railway יזהה אוטומטית שזה פרויקט Node ויריץ `npm start`.
6. תחת **Settings → Networking** לחץ **Generate Domain** כדי לקבל כתובת ציבורית.

אין צורך במשתני סביבה — Railway מספק את `PORT` והשרת מקשיב עליו לבד.

---

## מה כל קובץ עושה (בקצרה)

- **index.html** — כל הכלי בקובץ אחד: העיצוב (CSS), הממשק, וקוד ה‑JavaScript שממיר Markdown ומציג את הטקסט בבועת הודעה RTL. אין תלויות חיצוניות.
- **server.js** — שרת קטן שמגיש את `index.html`. נחוץ רק לסביבת שרת/Railway.
- **package.json** — מגדיר את פקודת `npm start` וגרסת Node.
- **railway.json** — אומר ל‑Railway איך לבנות ולהריץ.

---

## רעיונות להמשך

אם תרצה להרחיב את הכלי בעתיד:
- כפתור שמעתיק בחזרה את הטקסט המתוקן ללוח (מוכן להדבקה במייל/וורד).
- שמירת ההודעות בין רענונים (localStorage).
- כפתור מעבר בין מצב בהיר לכהה.

אלה שינויים קטנים ב‑`index.html` בלבד.
